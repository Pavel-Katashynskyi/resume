        <main> 
    {{#with person}}
        <div class="row m-3">
       <section class="col">
            {{> title big=true data='Person'}}
            <ul>
                <li><div>{{name}}</div></li>
                <li><div>Age: ({{age}})</div></li>
                <li><div>Gender: <b>{{gender}}</b></div></li>
            </ul>
            <div>
                {{#with address}}
                <b>Adress: </b> {{street}}, {{city}}, {{state}}, {{zip}}, {{country}}
                {{/with}}
            </div>
            <div class="mt-3">
                {{#each education}}
                    {{> title data= 'Education'}}
                    <ul>
                        <li>
                            <div>{{degree}}</div>
                            <div>{{major}}</div>
                            <div>{{university}} ({{graduationYear}})</div>
                        </li>
                    </ul>
                {{/each}}
            </div>
            <div>
                {{#each workExperience}}
                    {{> title data= 'workExperience'}}
                    <ul>
                        <li>
                            <div>{{company}}</div>
                            <div>{{title}}</div>
                            <div>{{startDate}} - {{endDate}}</div>
                            <div>{{responsibilities}}</div>
                            <div>Year founded: ({{year_founded}})</div>
                            <div>{{industry}}</div>
                            <div class="mt-3">
                                {{#each employees}}
                                    {{> title data= 'Employees'}}
                                    <div>{{name}}</div>
                                    <div>{{position}}</div>
                                    <div>{{departament}}</div>
                                        {{#each projects}}
                                            <h4 class="mt-3 mb-2">Projects</h4>
                                            <div>{{name}}</div>
                                            <div>{{description}}</div>
                                            <div>{{status}}</div>
                                        {{/each}}
                                {{/each}}
                            </div>
                        </li>
                    </ul>
                {{/each}}
            </div>
        </section>
        </div>
            {{/with}}    
        </main>


{{!--
Цей код містить лише те, що буде виводитись в {{{body}}} в layout/default.hbs
Адже наш layout/default.hbs має вже структуру HTML
--}}